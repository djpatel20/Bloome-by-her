VITE_API_URL=http://localhost:4000/api
# In Vercel (Preview + Production), set VITE_API_URL=/api instead — the
# /api rewrite in vercel.json proxies it to the Render backend so requests
# stay same-origin and auth cookies aren't blocked as third-party.
